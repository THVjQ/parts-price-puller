{
  "name": "parts-price-puller-web",
  "version": "2.0.0",
  "private": true,
  "description": "Self-hosted wholesale price matrix + per-store retail calculator (replaces the Google Sheet)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "CONFIG_DIR=../config DATA_DIR=../data AUTH_MODE=none GIT_SYNC=0 node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "express": "^4.21.1",
    "yaml": "^2.6.0"
  },
  "engines": { "node": ">=20" }
}
